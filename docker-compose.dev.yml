services:
  drivechronik-db:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: drivechronik
      POSTGRES_PASSWORD: drivechronik
      POSTGRES_DB: drivechronik
    volumes:
      - drivechronik-dev-db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U drivechronik"]
      interval: 5s
      timeout: 5s
      retries: 5

  teslamate-db:
    image: postgres:17-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: teslamate
      POSTGRES_PASSWORD: teslamate
      POSTGRES_DB: teslamate
    volumes:
      - teslamate-dev-db:/var/lib/postgresql/data
      - ./dev/fixtures/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U teslamate"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  drivechronik-dev-db:
  teslamate-dev-db:
